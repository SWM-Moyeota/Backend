plugins {
	id 'java'
	id 'org.springframework.boot' version '4.1.0'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'team.codingforest'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(25)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-oauth2-client'

	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'

	implementation 'io.jsonwebtoken:jjwt-api:0.12.3'
	implementation 'io.jsonwebtoken:jjwt-impl:0.12.3'
	implementation 'io.jsonwebtoken:jjwt-jackson:0.12.3'

	compileOnly 'org.projectlombok:lombok'
	annotationProcessor 'org.projectlombok:lombok'
	implementation 'org.springframework.boot:spring-boot-starter-security'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

	// 회원가입 입력값 검증(@NotBlank, @Email ...). 없으면 @Valid를 붙여도 아무 일도 일어나지 않는다.
	implementation 'org.springframework.boot:spring-boot-starter-validation'

	// 기본 DB: PostgreSQL (docker compose)
	runtimeOnly 'org.postgresql:postgresql'

	// h2 프로필 + 테스트용 인메모리 DB
	runtimeOnly 'com.h2database:h2'
	// Boot 4부터 H2 콘솔 자동설정이 별도 모듈로 분리됨 (스타터가 끌고 오지 않음)
	runtimeOnly 'org.springframework.boot:spring-boot-h2console'

	// Swagger UI (OpenAPI 문서 자동 생성)
	// 3.x가 Spring Boot 4 / Spring Framework 7 지원 라인. 2.x는 Boot 3용이라 호환되지 않는다.
	implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:3.1.0'

}

tasks.named('test') {
	useJUnitPlatform()
}