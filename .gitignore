### Spring Boot & Build Tools ###
/build/
/out/
.gradle/
/bin/
/target/

### IDE (IntelliJ, VS Code, Eclipse) ###
.idea/
*.iml
*.ipr
*.iws
.classpath
.project
.settings/
.vscode/
.factorypath

### HTTP Client ###
# .http 요청 응답을 파일로 저장(>>, >>!)할 때 생성되는 결과물
http/responses/

### OS temp files ###
.DS_Store
Thumbs.db

### Databases & Logs ###
# H2 데이터베이스를 파일 모드로 쓸 때 생성되는 로컬 DB 파일 방지
*.mv.db
*.trace.db
*.log

### Secrets & Local configs (선택) ###
# API 키나 DB 비밀번호가 들어간 로컬 설정파일이 있다면 주석을 해제해 무시하세요.
# application-local.yml
# application-secret.yml

### ai (선택)
*.ai
AGENTS.md

# 로컬 시크릿 설정
application-local.yaml
application-local.yml

### secrets 폴더 내에 json 파일
/secrets/
dump.rdb

# 부하 테스트 - seed 가 만드는 계정·토큰 파일
loadtest/users.json

# 모니터링 - exporter 접속 정보
infra/monitoring/.env

# 증거 캡처 이미지는 레포 밖에 보관 (txt 만 커밋)
docs/evidence/**/*.png
